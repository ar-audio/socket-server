{
  "name": "socket-server",
  "version": "0.0.1",
  "description": "Simple socket server to pass on position events",
  "dependencies": {
    "express": "^4.15.2",
    "socket.io": "^1.7.3"
  },
  "scripts": {
    "start": "node index.js"
  }
}
